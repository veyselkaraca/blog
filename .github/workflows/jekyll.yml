name: jekyll-cd

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: 🛎 Check out master
        uses: actions/checkout@master
        with:
          fetch-depth: 1
      - name: 🧪  Build and deploy
        uses: veyselkaraca/jekyll-actions@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_branch: 'gh-pages'